# --- Web playground (web.py) + RAG core ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
google-genai==1.73.1
python-dotenv==1.0.1
python-multipart==0.0.20
websockets==15.0.1

# --- WhatsApp webhook stack (app.py) — only needed if you run the WhatsApp bot ---
redis==7.4.0
rq==2.9.1
requests==2.33.1

# --- Document tooling (build_*.py scripts) — dev only ---
reportlab==5.0.0
pypdf
